# Codebase Map

Every file and folder in this project explained.
Updated every time a new file or folder is created.
Use this to navigate the codebase without reading all the code.

For feature descriptions → see `FEATURES.md`
For build progress → see `TASKS.md`
For infrastructure setup → see `SETUP.md`

---

## Root

```
ai-assistant/
  ├── FEATURES.md        — What every feature does and why (always up to date)
  ├── DESIGN_SPECS.md       — Detailed planning snapshot and design decisions
  ├── TASKS.md           — Build progress — what's done, what's next
  ├── CODEBASE.md        — This file — every file and folder explained
  ├── SETUP.md           — Step-by-step reusable infrastructure setup guide
  ├── docker-compose.yml — (created in Task 1.1) Docker container definitions
  ├── .env               — (created in Task 1.1) Environment variables and secrets
  └── .gitignore         — Files excluded from git (includes .env)
```

---

## Folders (added as they are created)

This section is populated during Phase 1 and beyond.
Every time you create a folder, add it here with a one-line description.

```
(empty — no folders created yet)
```

---

## How to update this file

When you create a new file or folder:
1. Find the right section above
2. Add one line: `filename — what it does`
3. If it connects to another file, note the connection

Keep descriptions short — one line per file is enough.
The goal is navigation, not documentation.
